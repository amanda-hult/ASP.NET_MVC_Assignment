<nav class="nav-links">
    <a class="nav-link @(Context.Request.Path == "/projects" ? "active" : "")" asp-controller="Admin" asp-action="Projects">
        <i class="fa-duotone fa-solid fa-briefcase"></i>
        <span>Projects</span>
    </a>

    @if (User.IsInRole("Admin"))
    {
        <a class="nav-link @(Context.Request.Path == "/members" ? "active" : "")" asp-controller="Admin" asp-action="Members">
            <i class="fa-duotone fa-solid fa-user-group"></i>
            <span>Team Members</span>
        </a>
    }

    <a class="nav-link @(Context.Request.Path == "/clients" ? "active" : "")" asp-controller="Admin" asp-action="Clients">
        <i class="fa-duotone fa-solid fa-handshake"></i>
        <span>Clients</span>
    </a>
</nav>
